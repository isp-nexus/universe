/**
 * @copyright OpenISP, Inc.
 * @license AGPL-3.0
 * @author Teffen Ellis, et al.
 */

// import { RefResolver } from "json-schema-ref-resolver"
// import { pluckDefaultExport } from '../environment.js'

// export async function resolveSchemas() {
// 	const schemaNameToPath = await import("@isp.nexus/schema/index.json", {
// 		with: {
// 			type: "json",
// 		},
// 	}).then(pluckDefaultExport)

// 	const schemas = await Promise.all(Object.values(schemaNameToPath).map(path => importJSONSchema(path)))

// 	for (const [schemaID, schemaPath] of Object.entries(schemaNameToPath)) {
// 		// resolver.
// 	}
// }
